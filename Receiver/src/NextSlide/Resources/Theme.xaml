<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        NextSlide dark theme — one file, kept as one file until it genuinely
        grows unwieldy (see README.md "Theming").

        Palette + fonts first. Every style below references these resources
        rather than hardcoding colors/fonts, so re-theming the app means
        editing only this block. Keys use a "Group.Name" convention
        (Bg.Base, Text.Primary, Status.Success, ...) purely for readability
        — WPF resource keys don't care about the dot.

        Palette matches the "Presenter Remote" reference page: a near-black
        base with a violet accent and a subtle indigo glow behind the
        content. Fonts are the one place this deviates from that reference
        — see the Font.Sans/Font.Mono comment below.
    -->
    <SolidColorBrush x:Key="Bg.Base" Color="#FF0A0B10" />
    <SolidColorBrush x:Key="Bg.Panel" Color="#FF14161F" />
    <SolidColorBrush x:Key="Bg.Elevated" Color="#FF1A1D29" />
    <SolidColorBrush x:Key="Border.Default" Color="#FF262A38" />
    <SolidColorBrush x:Key="Border.Strong" Color="#FF383E54" />
    <SolidColorBrush x:Key="Text.Primary" Color="#FFECEDF5" />
    <SolidColorBrush x:Key="Text.Secondary" Color="#FF8D92A8" />
    <SolidColorBrush x:Key="Text.Faint" Color="#FF565B70" />
    <SolidColorBrush x:Key="Accent" Color="#FF7C5CFC" />
    <SolidColorBrush x:Key="Accent.Hover" Color="#FF9D84FF" />
    <SolidColorBrush x:Key="Accent.Pressed" Color="#FF6247D9" />
    <SolidColorBrush x:Key="Accent.Soft" Color="#247C5CFC" />
    <SolidColorBrush x:Key="Status.Success" Color="#FF4ADE80" />
    <SolidColorBrush x:Key="Status.Warning" Color="#FFFFB74D" />
    <SolidColorBrush x:Key="Status.Error" Color="#FFFF6B6B" />
    <SolidColorBrush x:Key="Status.Info" Color="#FF64B5F6" />

    <!--
        Ambient top-of-window glow, standing in for the reference page's
        radial gradient glow (bg glow color fading to transparent by 65%
        of the ellipse radius) painted over its base background. Used as
        the Window background below instead of a flat Bg.Base fill.
    -->
    <RadialGradientBrush x:Key="Bg.Glow" Center="0.5,0.15" RadiusX="0.55" RadiusY="0.4">
        <GradientStop Color="#FF1A1030" Offset="0" />
        <GradientStop Color="#FF0A0B10" Offset="0.65" />
        <GradientStop Color="#FF0A0B10" Offset="1" />
    </RadialGradientBrush>

    <!--
        Fonts: the reference page uses Manrope (sans) + IBM Plex Mono, both
        loaded from Google Fonts. This template ships zero external assets
        (see README's "minimal dependencies" rule) and has no font files to
        embed, so it falls back to fonts that already ship with Windows —
        Segoe UI for the same "clean geometric sans" role, Consolas for the
        same "technical/monospace accent" role. To use the real fonts
        instead: add their .ttf files under Resources/Fonts/, mark them
        Build Action = Resource, and point these two keys at
        "pack://application:,,,/#Manrope" / "...#IBM Plex Mono" respectively.
    -->
    <FontFamily x:Key="Font.Sans">Segoe UI</FontFamily>
    <FontFamily x:Key="Font.Mono">Consolas, Cascadia Mono, Courier New</FontFamily>

    <!-- Window chrome: background + default foreground/font for the whole app. -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Bg.Glow}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="FontFamily" Value="{StaticResource Font.Sans}" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
    </Style>

    <!--
        Button — the neutral/secondary look (matches the reference's
        .btn-prev / .btn-goto: elevated surface, strong border, accent
        border + soft accent tint on hover). See Button.Primary below for
        the accent-filled look used on the main call-to-action.
    -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Bg.Elevated}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Strong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Accent}" />
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Accent.Soft}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Accent.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Bg.Panel}" />
                            <Setter Property="Foreground" Value="{StaticResource Text.Faint}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Button.Primary — the accent-filled look, for the one main
        call-to-action per screen (matches the reference's .btn-next
        gradient + glow). Opt in explicitly with Style="{StaticResource
        Button.Primary}"; everything else stays the neutral Button above.
    -->
    <Style x:Key="Button.Primary" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,9" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#7C5CFC" Direction="270" ShadowDepth="4" BlurRadius="16" Opacity="0.45" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" CornerRadius="10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FF9D84FF" Offset="0" />
                                <GradientStop Color="#FF7C5CFC" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Accent.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Bg.Panel}" />
                            <Setter Property="Foreground" Value="{StaticResource Text.Faint}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Bg.Base}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Strong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="CaretBrush" Value="{StaticResource Text.Primary}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Accent}" />
    </Style>

    <!--
        ComboBox — fully retemplated, not just property-styled. The default
        WPF ComboBox template draws its dropdown Popup with a separate,
        hardcoded system-colored Border (white background, system window
        frame border) that Background/BorderBrush property setters alone
        never reach — only the closed box picks those up. Left alone, that
        produces a dark closed box that opens onto a plain white dropdown.
        Retemplating the whole control (closed box + popup) the same way
        Button/TabItem/ScrollBar are handled above fixes that for good.
    -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource Bg.Elevated}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Strong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <!--
                        Visuals (MainBorder) and interaction (ToggleButton)
                        are deliberately two separate, same-size siblings
                        rather than one nested inside the other's own
                        template — nesting a visible Border inside a
                        ToggleButton's Template here didn't stretch to the
                        control's full width in practice. This split
                        matches how Button (which does render correctly) is
                        built: one plain Border owns 100% of the chrome.
                        The ToggleButton just sits on top, fully
                        transparent, to catch the click that opens/closes
                        the dropdown.
                    -->
                    <Grid>
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                                  x:Name="ContentSite"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center" />

                                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z"
                                      Fill="{StaticResource Text.Secondary}"
                                      Width="10" Height="5" Stretch="Uniform"
                                      Margin="0,0,10,0"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>

                        <ToggleButton x:Name="ToggleButton"
                                      Focusable="False"
                                      ClickMode="Press"
                                      Background="Transparent"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Stretch"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch" />
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource Bg.Elevated}"
                                        BorderBrush="{StaticResource Border.Strong}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,4,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="14" Opacity="0.4" />
                                    </Border.Effect>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource Accent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Text.Faint}" />
                            <Setter TargetName="MainBorder" Property="Background" Value="{StaticResource Bg.Panel}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Margin="2,1">
                        <ContentPresenter Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Accent.Soft}" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- CheckBox / RadioButton -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
    </Style>
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
    </Style>

    <!-- GroupBox -->
    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8" />
    </Style>

    <!-- TabControl / TabItem -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource Bg.Base}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
    </Style>
    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Secondary}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource Border.Default}"
                            BorderThickness="1,1,1,0"
                            CornerRadius="8,8,0,0"
                            Padding="{TemplateBinding Padding}"
                            Margin="0,0,2,0">
                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Bg.Elevated}" />
                            <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListView -->
    <Style TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!-- DataGrid (and its column headers / cells) -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowBackground" Value="{StaticResource Bg.Panel}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Bg.Elevated}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="RowHeaderWidth" Value="0" />
        <Setter Property="CanUserResizeRows" Value="False" />
    </Style>
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Bg.Elevated}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Secondary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8,4" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Accent}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ProgressBar -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource Bg.Elevated}" />
        <Setter Property="Foreground" Value="{StaticResource Accent}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!--
        ScrollBar — a minimal dark scrollbar: track + thumb only, no arrow
        buttons (the repeat buttons are transparent click regions for
        page up/down, not visible glyphs). Orientation is forwarded to the
        Track and the Width/Height triggers below so the same style works
        for both vertical and horizontal scrollbars.
    -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True" Orientation="{TemplateBinding Orientation}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Background="Transparent" BorderThickness="0" Command="ScrollBar.PageUpCommand" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="{StaticResource Border.Strong}" CornerRadius="6" Margin="2" />
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Background="Transparent" BorderThickness="0" Command="ScrollBar.PageDownCommand" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="12" />
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="12" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Separator / GridSplitter -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource Border.Default}" />
    </Style>
    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{StaticResource Border.Default}" />
    </Style>

    <!-- ToolTip -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource Bg.Elevated}" />
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="BorderBrush" Value="{StaticResource Border.Strong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
    </Style>

</ResourceDictionary>
